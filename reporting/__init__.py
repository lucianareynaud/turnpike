"""Reporting package for generating markdown reports from telemetry and eval artifacts.

This package is downstream-only and does not call providers, run routes, or execute evals.
It only reads existing artifact files and produces markdown reports.
"""
